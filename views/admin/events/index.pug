extends ../../layout
block content
    .events-page
        .topbar
            h1 events pannel
            .btn
                i(class="fa-duotone fa-plus")
                .
                    create new event
        hr
        each ev in events
            .events-container(id=ev._id)
                img(src="/images/events/resized/" + ev.image style="width:250px;height:15Opx;")
                .event
                    div="Event Name : " + ev.name
                    div="Date : " + ev.date.toISOString().substr(0, 10)
                    div="Address : " + ev.address
                .action
                    .btn Edit
                    .btn Delete
            hr
        .overlay
            .overlay-content
                .close
                    i(class="fa-duotone fa-2x fa-xmark")
                include create
    block script
        script(src="/javascripts/events.js")